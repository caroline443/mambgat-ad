# 核心依赖（服务器 Linux + CUDA 环境）
torch>=2.0.0
causal-conv1d>=1.2.0
mamba-ssm>=2.0.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# 配置 / 工具
pyyaml>=6.0
tqdm>=4.65.0
einops>=0.7.0
matplotlib>=3.7.0

# 安装说明：
# 1. 先装 PyTorch（根据服务器 CUDA 版本，V100 通常是 CUDA 11.x/12.x）
#    pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# 2. 再装 mamba-ssm（需要 Linux + NVIDIA GPU + CUDA >= 11.6）
#    pip install causal-conv1d mamba-ssm --no-build-isolation
#
# 3. 最后装其余依赖
#    pip install numpy pandas scikit-learn pyyaml tqdm einops matplotlib
