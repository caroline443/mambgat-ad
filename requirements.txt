# ═══════════════════════════════════════════════════════════════
# MambGAT-AD 依赖
# ═══════════════════════════════════════════════════════════════
#
# 安装说明：
#
# 【服务器 Linux + V100】
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   pip install causal-conv1d mamba-ssm --no-build-isolation
#   pip install -r requirements.txt
#
# 【Windows + Anaconda（无 mamba-ssm，自动 fallback）】
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#   （跳过 mamba-ssm，代码自动使用纯 PyTorch 实现）
# ═══════════════════════════════════════════════════════════════

# ── 必选（两端都需要）────────────────────────────────────────
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0
einops>=0.7.0
matplotlib>=3.7.0

# ── 可选：仅服务器 Linux + CUDA 环境（Windows 跳过）──────────
# causal-conv1d>=1.2.0
# mamba-ssm>=2.0.0
